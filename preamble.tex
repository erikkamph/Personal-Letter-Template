\usepackage[utf8]{inputenc}
\usepackage[swedish]{babel}
\usepackage{fancyhdr}

% For links
\usepackage{hyperref}
\usepackage{url}

% Forget page numbering since this is a letter when applying for jobs
\pagenumbering{gobble}

%\pagestyle{fancy}
%\fancyhf{}
%\lhead{Namn\\Personnummer}
%\rhead{E-post\\Telefon}

% For random nonsense text that does not mean anything, but just fill the template
\usepackage{blindtext}